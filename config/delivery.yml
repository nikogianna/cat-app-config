apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: cat-app
  labels:
    app.kubernetes.io/part-of: tanzu-cat-app
    apps.tanzu.vmware.com/workload-type: web
    app.kubernetes.io/component: run
    carto.run/workload-name: cat-app
spec:
  template:
    metadata:
      annotations:
        conventions.apps.tanzu.vmware.com/applied-conventions: ""
        developer.conventions/target-containers: workload
      labels:
        app.kubernetes.io/component: run
        app.kubernetes.io/part-of: tanzu-cat-app
        apps.tanzu.vmware.com/workload-type: web
        carto.run/workload-name: cat-app
    spec:
      containers:
      - env:
        - name: JAVA_TOOL_OPTIONS
        image: devworkspaceacr.azurecr.io/samples/cat-app-flux-system@sha256:c197d4bee1b9cfbc510292ba057dd458ee69a278b89f327b9e004087836b0fbf
        name: workload
        resources: {}
        securityContext:
          runAsUser: 1000
      serviceAccountName: default

